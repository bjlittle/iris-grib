version: 2

formats:
  - pdf

conda:
  environment: docs/environment.yml

python:
  version: 3.7
  install:
    - method: pip
      path: .
      extra_requirements:
        - scitools-iris
        - cartopy
